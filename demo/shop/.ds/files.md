# shop — files

Which feature owns each file. Look here before you edit, to find the feature file to update.

`src/api/routes.ts` → HTTP API
`src/cart/cart.ts` → Cart
`src/cart/line.ts` → Cart
`src/catalog/product.ts` → Promotion catalogue
`src/catalog/promotion.ts` → Promotion catalogue
`src/checkout/index.ts` → Checkout
`src/checkout/session.ts` → Checkout
`src/checkout/validate.ts` → Checkout
`src/orders/place.ts` → Orders
`src/orders/refund.ts` → Orders
`src/payments/charge.ts` → Orders
`src/pricing/discount.ts` → Apply discount
`src/pricing/rules.ts` → Apply discount
`src/pricing/tax.ts` → Order totals
`src/pricing/totals.ts` → Order totals
